name: Apply WIP motif planner

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: wip-motif-planner
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Apply planner patch
        run: python tools/apply_wip_motif_planner.py
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt pytest scipy pillow reportlab
      - name: Run tests and corpus gates
        run: python -m pytest tests/ -q
      - name: Commit clean implementation
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git rm -f .github/workflows/apply-wip-motif-planner.yml tools/apply_wip_motif_planner.py
          git add wipple/accounting/wip.py tests/test_wip_motif_planner.py
          git commit -m "Plan WIP mappings from structural motifs"
          git push origin HEAD:wip-motif-planner
